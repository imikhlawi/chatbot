# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Jupyter
.ipynb_checkpoints/

# Modell (nicht hochladen – zu groß, Nutzer laden selbst)
llm_model/*.gguf
llm_model/*.bin
storage/models/*.gguf
storage/models/*.bin

# Chroma persistent (lokal befüllt)
storage/chroma/*
!storage/chroma/.gitkeep

# PDF-Dateien (nicht hochladen – Nutzer legen eigene PDFs in data/)
data/*.pdf

# Vector-Store (wird lokal aus PDFs neu erzeugt)
faiss_index/

# Caches
.cache/
*.cache
llm_model/.cache/

# Umgebung / IDE
.venv/
venv/
.env
.env.local
*.swp
*.swo

# API (sensible Konfiguration bleibt lokal)
apps/api/.env

# OS
.DS_Store
Thumbs.db

# Temporäre Testdateien (z. B. curl -d @body.json)
body.json
*.tmp
*.temp
*.bak
*.log

# Node (Build / Dependencies)
node_modules/
.next/
out/

# Intern / nur für lokale Entwicklung (nicht ins Repo)
TEST-BEFEHLE.md
BEFEHLE-KI-TEST.md
scripts/
