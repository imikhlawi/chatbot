# API Base URL (lokal: localhost:8000; im Browser muss die API vom Rechner erreichbar sein)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: wenn API_KEY in der API (.env) gesetzt ist
# NEXT_PUBLIC_API_KEY=dein_key
